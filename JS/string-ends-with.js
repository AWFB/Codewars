// endsWith, startsWith, includes, 
function solution(str, ending){
    if (str.endsWidth(ending))
      return true
    else 
      return false    
  }

  // Cleaner examples
function solution(str, ending){
    return str.endsWith(ending);
}

const solution = (str, ending) => str.endsWith(ending);